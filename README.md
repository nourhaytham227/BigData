# BigData

Worked on a dirty dataset of movies and information about them. The dataset initially had missing values, inconsistent data types and null values. A solution was built using Python, Kafka and NiFi to build an ETL pipeline.
